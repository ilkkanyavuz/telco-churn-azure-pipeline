# Excel exploration

Before moving to Power BI, an initial exploration and simple dashboard were built in Excel (pivot tables, slicers, basic KPIs). The screenshot in `excel_screenshots/` shows this view.